const { } = require("../models");
const { signToken, authenticationError } = require("../utils/auth");

const resolvers = {
    Query: {
        
    },
    Mutation: {

    }
}

module.exports = resolvers;